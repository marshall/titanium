#!/usr/bin/env python
import os.path as path

Import('build')
csbuilder = Builder(action = 'devenv $SOURCE /rebuild Release')

Env = build.env.Clone(BUILDERS = {'CSProgram': csbuilder})
Env.Append(CPPPATH=[build.kroll_source_dir+'/api'])

out = path.join(build.runtime_build_dir, 'installer', 'Installer.exe')
p = Env.CSProgram(out, 'Installer.sln')
build.mark_build_target(p)
